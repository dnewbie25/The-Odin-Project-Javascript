let add7 = num => num + 7;

let multiply = (a, b) => a * b;

let capitalize = function(str){
    capitalized = str[0].toUpperCase() + str.slice(1, str.length).toLowerCase()
    return capitalized
};

let lastLetter = (str) => {
    return str[str.length-1]
};